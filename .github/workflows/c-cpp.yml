name: C/C++ CI

on:
  push:
    branches: [ "unicorn" ]
  pull_request:
    branches: [ "unicorn" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: make
      run: make

