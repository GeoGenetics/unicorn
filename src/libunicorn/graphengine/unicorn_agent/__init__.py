"""Backend-owned Unicorn Graph Engine Agent runtime.

The package is intentionally independent from the browser runtime.
Provider-neutral contracts live in ``unicorn_agent/schemas`` and are the only
authoritative boundary definitions for this package.
"""
